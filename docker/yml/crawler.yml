x-crawler-common: &crawler-common
    restart: unless-stopped
    networks:
        - hdfs_network

services:

    realestate-crawler-service:
        <<: *crawler-common
        build:
            context: ../../crawler
        image: realestate-crawler:latest
        container_name: realestate-crawler

networks:
    hdfs_network:
        external: true
